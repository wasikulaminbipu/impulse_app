# frozen_string_literal: true

json_key = ENV["PLAY_STORE_JSON_KEY"] || ENV["JSON_KEY_FILE"] || ENV["SUPPLY_JSON_KEY"] || "pc-api-key.json"
json_key_file(json_key) if File.exist?(json_key)

package_name("com.impulseagriscienceltd.impulse_app")
